# Result Artifacts

Expected generated file:

- `all_predictions.pkl`: saved model predictions and metadata used by the evaluation notebook.

This file is not present until the model/evaluation notebook exports it. Keep it small enough for GitHub, or replace it with a CSV/Parquet sample if it grows too large.
